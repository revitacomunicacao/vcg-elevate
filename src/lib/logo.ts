/** Dimensões intrínsecas dos arquivos de logo em `src/assets` (sem redimensionar via CSS). */
export const LOGO_INTRINSIC_WIDTH = 239;
export const LOGO_INTRINSIC_HEIGHT = 86;

/** Altura útil da navbar: logo + `py-2` (8px + 8px). */
export const NAVBAR_OFFSET_PX = LOGO_INTRINSIC_HEIGHT + 16;
